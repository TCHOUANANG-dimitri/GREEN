# ============================================================
# GREEN App — Backend Dependencies
# Install with: pip install -r requirements.txt
# ============================================================

# PyTorch CPU-only wheel index (avoids downloading ~1.4 GB of CUDA libs on CPU-only servers)
--extra-index-url https://download.pytorch.org/whl/cpu

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database (SQLite via SQLAlchemy + PostgreSQL en production)
sqlalchemy==2.0.35
psycopg2-binary==2.9.10

# Authentication (JWT + Password hashing)
python-jose[cryptography]==3.3.0
bcrypt==4.2.1          # Used directly — passlib has a bug with bcrypt>=4.x

# Form data / file uploads
python-multipart==0.0.12

# Static file serving
aiofiles==24.1.0

# Environment variables
python-dotenv==1.0.1

# HTTP client (for drone stream fetching, weather API)
httpx==0.28.1
requests==2.32.3

# Image processing (for AI inference on drone frames)
Pillow==11.1.0

# AI model inference (EfficientNet — best_efficientnet.pth)
torch==2.13.0+cpu
torchvision==0.28.0+cpu
timm==1.0.27

# Numpy (for embeddings / RAG)
numpy==2.2.6

# TF-IDF RAG (for GreenBot retrieval)
scikit-learn==1.6.1

# Gemini AI API (GreenBot chatbot + disease research)
google-genai==1.16.1
